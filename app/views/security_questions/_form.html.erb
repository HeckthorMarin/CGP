<%= form_with(model: security_question, local: true) do |form| %>
  <% if security_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(security_question.errors.count, "error") %> prohibited this security_question from being saved:</h2>

      <ul>
      <% security_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :question %>
    <%= form.text_field :question, id: :security_question_question %>
  </div>

  <div class="field">
    <%= form.label :answer %>
    <%= form.text_field :answer, id: :security_question_answer %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
