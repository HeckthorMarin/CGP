<%= form_with(model: icon, local: true) do |form| %>
  <% if icon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(icon.errors.count, "error") %> prohibited this icon from being saved:</h2>

      <ul>
      <% icon.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ref_icon %>
    <%= form.text_field :ref_icon, id: :icon_ref_icon %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
